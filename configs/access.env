# Provider
ACCESS_PROVIDER=cloudflare

# quick | production
ACCESS_MODE=quick

# Local service to expose
TARGET_HOST=localhost
TARGET_PORT=8000

# Cloudflare
CLOUDFLARED_BIN=cloudflared

# Runtime
PID_FILE=runtime/cloudflared.pid
LOG_FILE=runtime/cloudflared.log
URL_FILE=runtime/cloudflared.url
